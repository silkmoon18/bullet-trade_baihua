# BulletTrade 项目协作规则

## 聚宽策略与运行时边界

以下规则适用于 `strategies/joinquant/`、`helpers/bullet_trade_jq_remote_helper.py`
以及相关测试、导出脚本和文档。

### 聚宽策略文件只表达决策

`strategies/joinquant/*.py` 必须只保留会改变策略结果的决策代码，以及聚宽运行所必需的最薄入口：

- 允许保留标的池、数据时间边界、过滤条件与顺序、因子计算、排序、入选数量、目标权重、止盈止损阈值和决策时间；
- 允许保留 `initialize`、`process_initialize` 和定时回调等聚宽要求的顶层入口，但入口只负责声明策略参数、调用决策代码并委托 helper；
- 允许保留最少量的策略身份和运行时安装声明，例如 `STRATEGY_ID`、helper API 版本及私有配置模块名；
- 不得包含 JQ/QMT 账户分支、资金和持仓账本、对账、RPC/TCP、重试、幂等、订单状态推进、下单类型实现、追单规则、通知排版、名称缓存、凭据、配置解析或序列化；
- 不得直接设置聚宽平台通用参数、交易成本、滑点或注册调度；这些操作由 helper 的门面完成；
- 策略向 helper 只提交账户无关的决策结果，例如目标权重、参考价、止盈止损阈值和幂等键，不读取或拼装基础设施内部状态。

若一段代码被删除或移动后不会改变入选标的、排序、目标权重或风控决策，它默认不属于策略文件，应放入 helper 或服务器实现。

### 重构不得暗改选股结果

移动基础设施代码时，不得顺带改变以下行为：

- 数据日期和实时/历史数据边界；
- 标的池来源；
- 过滤条件、过滤顺序及异常降级语义；
- 因子公式、排序方向、入选数量；
- 权重归一化公式和总部署比例。

每次边界重构必须保留或新增契约测试，至少覆盖多标的过滤、排序、入选结果和精确权重。确需改变策略结果时，必须作为独立的策略变更说明和测试，不能混入基础设施重构。

### helper 的源码和部署形态

聚宽研究环境中的正式部署物必须始终是研究根目录下可独立导入的单文件：

`bullet_trade_jq_remote_helper.py`

策略只能使用 `import bullet_trade_jq_remote_helper as bt`，不得依赖聚宽研究目录中的包、子目录或相对导入。

helper 可以为了本地维护而拆分源码，但必须满足以下条件：

- 拆分后的本地模块不得成为聚宽运行时依赖；
- 由确定性构建脚本生成单文件 helper，生成物必须可审查、纳入版本控制并由导出门禁检查是否最新；
- 生成后的单文件必须保持 Python 3.8 语法兼容，并能在缺少 `dataclasses` 等聚宽未提供模块时独立导入；
- `.pyi` 描述部署单文件的公共接口，而不是暴露内部模块；
- `scripts/export_joinquant.py` 只导出通过一致性检查的单文件 helper，不把源码目录上传到聚宽；
- 拆分不能增加策略文件中的导入、适配或基础设施代码。

在没有单文件构建与一致性门禁前，不得直接把现有 helper 拆成聚宽需要分别导入的多个文件。

建议的本地职责边界最多保持为少量粗粒度模块：执行数据模型、RPC与配置、目标/账户运行时、聚宽门面。不要为每个类或函数建立单独模块。
