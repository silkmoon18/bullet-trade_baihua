# 固定价格执行：ETF直接挂单、股票等待价格笼子

日期：2026-09-04。状态：本地修正，未部署；以本页替代上一轮未发布的“实时对手价自适应限价”方案。

## 决策与不变项

用户确认：策略决定期望价格，执行层只负责该价格何时可以申报及委托后的处理。不得因为交易所规则改用卖一/买一或笼子边界定价。

- GoodETF调仓买入仍为开盘后策略取得的 `last_price × 1.002`。
- 止盈仍为首次触发时参考价 `× 0.998`，不是全天固定用开盘价止盈。
- 调仓清仓/减仓和止损仍是原有市价IOC及当日补剩余量；先卖后买不变。
- 原报价精度、涨跌停处理、资金/归属校验仍保留。
- 选股池、过滤顺序、折价公式、排序、权重、风控阈值、JQ撮合和通知归属不变。
- 删除未部署的 `AdaptiveLimitExecution`、盘口重定价和额外执行协议升级；复用 `LimitExecution(2000)`，执行schema仍为v2，兼容v1。helper API仍为本次待发布的v18。

## 现在怎样执行

1. 策略提交目标权重和参考价；服务器按策略归属资金及该参考价计算目标数量，并持久化参考价。
2. `KEEP_ORIGINAL`下，等待、部分成交后的补单和笼子废单重试都使用初次参考价与相同偏移，不用后续tick的last_price重新定价。
3. ETF：直接提交原定限价，不再等待卖一回到买价以下/买一回到卖价以上；无成交时保持原委托排队。股票动态笼子不适用于ETF。
4. 股票：根据最新行情及板块计算可申报范围。原定价格在笼子内则原价申报，笼子外则等待下一笔行情，不把订单价格钳制到笼子边界。
5. 已报委托保持排队，部分成交只入账已成交量；上一笔终态后才按追单选项处理剩余量，防止重复活动委托。
6. 股票限价明确因笼子废单：保存拒单原因，等待更新tick，再检查原价是否可申报。重复目标请求也不能绕过这个检查。
7. 资金、权限、数量、未知原因等非笼子拒单停止自动重试并记录原因。通信结果未知仍走原有幂等/对账，不能当成确定拒单重发。
8. `FollowUpPolicy.NONE`不补终态剩余量；GoodETF的`UNTIL_FILLED_TODAY`只在当日处理。有效限价单不会因为价格变化而自动撤单提价；午夜关闭旧日目标。
9. 其他策略仍可以显式选择`ConditionalLimitExecution`或`RECOMPUTE`，其主动等待/重新定价语义不变；GoodETF不使用它们。

例如：原定买价10.02元，股票当前笼子上界9.89元，则等待；上界涨至10.09元后，仍报10.02元。ETF原定买价1.002元、卖一1.050元，则直接挂1.002元等成交，不报1.050元。

## 保留的T+0及行情工作

- 直连QMT通过`T+0基金`板块识别可当日回转品种；明确识别的新买入成交当日可卖，其余/识别失败按T+1。板块按日缓存，重启可重试。
- 不回溯修改已经记账批次的可卖日，不借用手工持仓。最终可卖量仍受策略归属与券商实际可卖量共同约束。
- 原生tick缺少元数据时保留同一天已查询信息，不跨日沿用涨跌停数据。
- 不按ETF名称或上市日期猜测无限制。QMT没有返回有效上下限，不代表已经证明交易所无涨跌幅限制。
- 未修改独立回测引擎的通用基金默认配置。

## 分片与审查

| 分片 | 核验点 |
|---|---|
| A：还原定价 | 买入/止盈偏移不变，JQ路径不变，删除盘口重定价 |
| B：申报门槛 | ETF无需报价触发即可挂原价；股票等待笼子后原价提交 |
| C：拒单与回调 | 笼子废单等更新行情，重试仍原价；非笼子拒单不循环；有效单不重复挂 |
| D：兼容与文档 | 单文件helper导出、Python 3.8兼容、选股及精确权重契约、T+0账本、旧协议兼容 |

新增测试明确覆盖：ETF卖一高于原买价仍直接下单；股票价格出笼/入笼；等待中行情变化不改变已保存参考价；废单重试价格一致；原生tick推进股票等待目标；适配器最终价格不跟随盘口变化。

本地回归444项通过，聚宽单文件导出门禁通过，git差异空白检查通过。测试使用模拟broker，不代表已经做过QMT柜台实单验收。

## 待部署顺序

1. 先更新fork服务器，不修改账户开关或重置账本。
2. 聚宽研究根目录替换helper v18，并更新各策略的`good_etf.py`（保留各自STRATEGY_ID）。
3. 私有`jq_runtime_config.py`无需新增字段、修改模式或凭据。
4. 暂停/重启聚宽策略使新helper生效；已有持久化意图保留原执行类型，新目标才使用固定限价类型，不能暗改在途委托。
5. 模拟账户验收固定价格、实际委托/成交、归属记账、T+0与午夜目标清理。没有保证当日必然成交。

## 规则依据与纠正

- [上交所交易规则（2026年修订）](https://www.sse.com.cn/lawandrules/sselawsrules2025/stocks/exchange/c/c_20260424_10816482.shtml)：股票动态价格范围、科创板特别规定、基金涨跌幅及报价规则是不同约束。
- [深交所价格笼子说明](https://investor.szse.cn/index/update/t20230713_601728.html)、[北交所交易规则](https://www.bse.cn/uploads/6/file/public/202111/20211119182131_imqohh30d0.pdf)：股票连续竞价边界按市场配置。
- [上交所债券ETF指南](https://etf.sse.com.cn/ruleguide/guide/c/5287915.shtml)、[黄金ETF指南](https://www.sse.com.cn/assortment/fund/etf/rules/c/c_20150911_3985190.shtml)：不能把债券/黄金ETF一律视为无涨跌幅限制。
- [深交所ETF上市说明](https://www.szse.cn/aboutus/trends/news/t20060424_517452.html)：有上市首日即设置涨跌幅限制的ETF，不能用上市日期统一豁免。
- [迅投xtquant更新记录](https://dict.thinktrader.net/nativeApi/download_xtquant)：提供`T+0基金`板块；实际名单以运行环境返回为准。
