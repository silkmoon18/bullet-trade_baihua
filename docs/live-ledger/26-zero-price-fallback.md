# 2026-09-07：缺价成交按0记账

> 历史部署记录，不代表当前代码口径。现行零价兜底：成交股数照常入账，资金按参考价暂算，精确收益不可用；参见 [45-fill-price-recovery.md](45-fill-price-recovery.md)。

## 用户决策与范围

用户明确要求“没有任何有效数据就记为0”。本次针对已确认归属、方向、成交数量和成交ID有效，但价格资料缺失的成交；不是把缺失数量、ID或费用一律改成0。

只修改服务器记账，不改变策略决策、买卖定价、订单类型或追单机制；聚宽策略、helper和私有JQ配置不需要为本次修改更新。

## 启用与行为

服务器`.data/.env`使用：

```dotenv
QMT_STRATEGY_UNPRICED_FILL_POLICY=ZERO_FALLBACK
```

沿用现有配置入口，只增加一个枚举值。默认STRICT仍拒绝缺价成交；CONSERVATIVE_ORDER_PRICE仍只允许原先的委托价估算。

ZERO_FALLBACK依次尝试：有效成交价；有效成交金额÷数量；满足原有整笔成交条件的有效委托价。都不可用才记录`price_units=0, price_source=ZERO_FALLBACK, price_known=false`。

- 成交数量按回报更新持仓，重复回报不重复入账或重复通知。
- 零价买入不扣本金；零价卖出不增加本金现金。已知佣金/税费仍扣除，未知费用保持未知。
- 飞书成交卡片显示0，并说明“按0记账（非真实成交价），资金/收益不准确”。精确NAV、收益等仍保持未知，不能伪装成真实业绩。
- 不移除资金容量、持仓归属、数量、成交关联等检查。零价买入可能使策略现金高于真实可用现金而再次触发现金不足；零价卖出也不会恢复实际卖回的买入资金。成本失真可能影响止盈止损；均价为0时现有helper跳过基于成本的阈值判断。
- 后续若同一笔已按0入账的成交返回不同有效价格，沿用成交冲突提示，不静默改写历史账本；正确恢复需要独立校正。之前已经估算或真实入账的成交，本次不主动重算。

## 实现与验收

1. 成交证据与领域模型只允许带显式ZERO_FALLBACK且价格未知标记的0价。
2. 新增SQLite v10迁移：事务内复制全部fills数据、重建价格约束与索引；不改旧迁移校验和，不删除历史成交。部署前备份账本，回滚需同时恢复备份。
3. 已知费用允许从零价卖出后的现金扣除，其余余额约束保留。
4. 测试覆盖取价优先级、缺失非价格证据仍拒绝、买卖记账、已知/未知费用、部分成交、重复回报、旧账本迁移、通知与精确收益不可用。

407项账本、QMT适配、聚宽运行时、策略契约和导出测试通过；关键语法/未定义名称检查与`git diff --check`通过。复审确认默认策略不变、只有显式零价标记可保存0、历史迁移校验和不变。

## 部署状态

实现验收时仅本地修改与测试；随后按用户“部署”指令，于2026-09-07 21:26部署代码提交`21a8456`到服务器fork。

- 停服前后均备份，停服后账本备份为`.data/backups/strategy-ledger-20260907-212601-127244.db`；配置备份为`env-before-zero-fallback-20260907-212328.env`。
- 从`19e3516`快进到`21a8456`，只修改`.env`的`QMT_STRATEGY_UNPRICED_FILL_POLICY`为ZERO_FALLBACK；全局交易保持true，白名单仍只有`good_etf_remote`。
- 服务58620恢复监听、QMT ready；QMT进程未重启。数据库v10、quick_check=ok、外键检查无错误；14笔原成交逐字段保留。159031及159033两笔卖出以0价/价格未知新增入账，未手工改账。
- **交易验收未通过**：重启后原有回调执行逻辑自动推进当日剩余目标，新增3笔买入尝试，账本状态均REJECTED，没有新增买入成交记录。3笔尝试的broker_order_id分别与同日已有卖单重复，导致后续3笔历史卖出被关联到错误本地订单，报`broker fill id was reused with different fields`。原成交的价格、数量、方向、时间等字段核对没有变化，不能再归因于缺价。
- 现有`broker.orders`查询也出现新client_tag与旧卖单证券/filled状态组合，说明同日ID复用下缓存和归属关联不可靠，不能把这些行当作新买单成交证据。账本活动订单数为0，但`strategy_ledger_ready=false`，不能宣称交易链路正常。
- 未主动调用下单或撤单接口；上述买入尝试来自保持交易开启时的自动恢复。旧上游目录仍为`cd42a97`且原有`logs/app.log`/`command.txt`状态未变，未触碰。

后续需要单独修复收盘后自动推进发单及同日委托ID复用的关联规则；本轮部署未扩大修改范围。零价规则无须更新聚宽文件，但随本轮一并部署的上一提交`9a5c31f`包含HK/沪深港过滤修复，聚宽侧仍需用户上传新版helper才能生效。没有要求用户导出成交记录，也没有补回真实成交金额。

## 后续变更

- 2026-09-16：曾按“参考价 ×(1 ∓ 保护带)”估算并补足账本金额，历史经过见 [44-unpriced-fill-estimates.md](44-unpriced-fill-estimates.md)。该做法后来再次修订；当前执行规则、历史暂估款处理与自动价格更正见 [45-fill-price-recovery.md](45-fill-price-recovery.md)。
